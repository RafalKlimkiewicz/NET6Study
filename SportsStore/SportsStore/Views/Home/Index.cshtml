@model IQueryable<Product>

<h1>Welcome to SportsStore</h1>

@foreach (var p in @Model ?? Enumerable.Empty<Product>())
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("")</h4>
    </div>
}

